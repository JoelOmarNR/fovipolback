spring.application.name=logistica
#server.port=${PORT:0}
server.port=8001

##Conexion DB ORACLE
spring.datasource.url=jdbc:oracle:thin:@192.168.10.59:1521:SIFOPROD
spring.datasource.username=SISFOV
spring.datasource.password=DESSISFOV23
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver


# ===============================
# = JPA / HIBERNATE
# ===============================
# Show or not log for each sql query
#spring.jpa.show-sql=true

# Hibernate ddl auto (create, create-drop, update): with "create-drop" the database
# schema will be automatically created afresh for every start of application

#spring.jpa.hibernate.ddl-auto=create-drop

# Allows Hibernate to generate SQL optimized for a particular DBMS

#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle10gDialect

#CONFIG LOG APP ========================================================================

#logging.level.root=error
#logging.level.org.springframework.web=error
#spring.data.rest.basePath=/logistica

# Disabling the /v3/api-docs endpoint
#springdoc.api-docs.enabled=true
# Disabling the swagger-ui
#springdoc.swagger-ui.enabled=true
# swagger-ui custom path
#springdoc.swagger-ui.path=/swagger-ui/index.html


